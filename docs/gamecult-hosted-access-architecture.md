# GameCult Hosted Access Architecture

This shared design moved.

Canonical location:

- `E:\Projects\Heimdall\docs\architecture.md`

Repixelizer-specific binding:

- `E:\Projects\Heimdall\docs\app-profiles\repixelizer.md`

This local file remains only as a redirect stub so old notes and links do not
pretend the shared access plan still lives inside Repixelizer.

Current repo truth is unchanged:

- the hosted demo here still only has queue and limit machinery
- no Discord login, Patreon login, linked identities, local sessions, or access
  gates are landed in `E:\Projects\repixelizer`
